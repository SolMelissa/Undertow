# Hydrus patches

Undertow runs Hydrus from the winget-installed release
(`%USERPROFILE%\HydrusPipeline\hydrus\hydrus_client.exe`), not a source fork. Any custom
behavior we want in Hydrus itself gets applied here as a patch/script against that installed
release, rather than maintaining a full source checkout in this repo.

Nothing lives here yet.
